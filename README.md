# orders-service

A minimal FastAPI CRUD API. Run `docker compose up --build`, then visit `/health` or `/items` on port 8001. Branches named `fg/<feature>` join the matching feature group in `catalog-service`; all other branches get an independent preview.
